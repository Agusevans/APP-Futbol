<!doctype html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>APP Fútbol</title>

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Favicons -->
    <link rel="icon" href="/images/pelota.png" sizes="32x32" type="image/png">

    <style>
        body {
            background: url('/images/fondo-futbol.jpg') no-repeat center center fixed;
            background-size: cover;
            padding-top: 70px;
            font-family: 'Poppins', sans-serif;
            color: white;
            text-align: center;
        }

        .card-container {
            max-width: 600px;
            margin: auto;
            padding: 30px;
        }

        .card {
            border-radius: 15px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.4);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(12px);
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .card:hover {
            transform: scale(1.07);
            box-shadow: 0px 6px 25px rgba(0, 0, 0, 0.5);
        }

        .card a {
            display: block;
            text-decoration: none;
            color: white;
            font-weight: bold;
            font-size: 1.2rem;
            text-align: center;
            padding: 12px 20px;
            border-radius: 8px;
            margin: 8px 0;
            background-size: cover;
            transition: all 0.3s ease-in-out;
            width: 100%;
        }

        .card a:hover {
            filter: brightness(1.2);
            transform: scale(1.05);
        }

        .title {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 25px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
        }

        .navbar {
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
            background: rgba(0, 0, 0, 0.85) !important;
        }

        .btn-logout {
            transition: background 0.3s ease-in-out;
        }

        .btn-logout:hover {
            background: red;
            color: white;
        }
    </style>
</head>

<body>
<!-- HEADER -->
<nav class="navbar navbar-expand-md navbar-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="/index">🏠 Home</a>
        <div class="d-flex">
            <span class="navbar-text me-3">👤 Hola {{usuario}}</span>
            <a class="btn btn-outline-light btn-logout" href="#" onclick="confirmLogout()">Cerrar Sesión</a>
        </div>
    </div>
</nav>
<!-- END HEADER -->

<!-- BODY -->
<main class="container">
    <div class="card-container">
        <h1 class="title">Seleccione una acción</h1>
        <div class="card">
            <a href="partidos">📜 Ver historial de partidos</a>
            <a href="jugadores">👥 Ver jugadores</a>
            <a href="canchas">🏟️ Ver canchas</a>
            {{#if admin}}
                <a href="partidos/gestion">⚽ Alta de partido</a>
                <a href="jugadores/gestion">➕ Alta de jugador</a>
                <a href="canchas/gestion">➕ Alta de cancha</a>
            {{/if}}
        </div>
    </div>
</main>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
        function confirmLogout() {
            if (confirm("¿Estás seguro de que deseas cerrar sesión?")) {
                window.location.href = "/logout";
            }
        }
    </script>
</body>

</html>
